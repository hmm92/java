CREATE TABLE USERS (
                        ID SERIAL,
                        USER_NAME varchar,
                        PASSWORD varchar,
                        FIRST_NAME varchar,
                        LAST_NAME varchar,
                        ROLE varchar
);
